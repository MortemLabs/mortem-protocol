# SDK package environment template. MORTEM_MASTER_KEY is optional and enables AES-256-GCM payload
# encryption when callers do not pass a key directly.

# Generate with: openssl rand -base64 32
MORTEM_MASTER_KEY=
