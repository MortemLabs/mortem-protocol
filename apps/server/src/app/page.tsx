// The server app root confirms the API service is reachable during deployments. Product UI is built
// in the separate dashboard app.
export default function ServerRootPage() {
  return <main>Mortem server online</main>
}
